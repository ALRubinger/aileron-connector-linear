# Aileron suite manifest for linear-issues-and-comments.
#
# Generated by aileron-codegen. Lists every action emitted for this
# connector so `aileron action add-suite` installs the full
# surface in one shot. Re-run `task generate` after adding or
# removing operations from gen.yaml.

name = "linear-issues-and-comments"
description = "Read and write to a Linear workspace: fetch an issue by id, look up the authenticated user, create issues, and add comments. Writes are gated on per-call user approval per ADR-0009."

actions = [
  "actions/comment-create",
  "actions/get-issue",
  "actions/issue-create",
  "actions/viewer",
]
